# RPG Resource Tracker

A small, client-side React application for tracking resources for tabletop RPG characters. Data stays in the browser through `localStorage`.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

GitHub Actions deploys the generated `dist` directory to GitHub Pages on pushes to `main`.
